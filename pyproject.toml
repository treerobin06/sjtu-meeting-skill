[project]
name = "sjtu-meeting-skill"
version = "0.1.0"
description = "Agent skill and Python CLI for managing SJTU cloud video meetings through the meeting.sjtu.edu.cn backend API."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "httpx>=0.27",
]

[project.scripts]
sjtu-meeting = "scripts.sjtu_meeting:main"
